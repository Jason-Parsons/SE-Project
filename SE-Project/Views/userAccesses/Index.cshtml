@model IEnumerable<SE_Project.Models.userAccess>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserAccess1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccessLevel)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserAccess1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccessLevel)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.UserAccess1 }) |
                @Html.ActionLink("Details", "Details", new { id = item.UserAccess1 }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.UserAccess1 })
            </td>
        </tr>
    }

</table>
